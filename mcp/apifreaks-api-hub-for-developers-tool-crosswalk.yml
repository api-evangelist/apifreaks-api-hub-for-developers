generated: '2026-09-04'
method: derived
source: https://github.com/api-freaks/apifreaks-mcp (src/tools/*.ts + src/endpoints.ts, @apifreaks/mcp v2.0.1) bound
  against openapi/ (27 refined OpenAPI 3.2 documents, 108 operations)
summary: 'Every tool in the official APIFreaks MCP server v2.0.1 is bound to the REST operation it actually calls.
  The binding is not by name similarity: the server ships a literal ENDPOINTS map (src/endpoints.ts) of tool ->
  URL path, so each row below is a resolved path+method match against the published OpenAPI, not a guess. All 109
  tools resolve. Two published REST operations have no tool.'
surfaces:
  openapi:
    files: 27
    operations: 108
    gated: false
    location: openapi/
  mcp:
    package: '@apifreaks/mcp'
    version: 2.0.1
    transport: stdio
    url: null
    gated: false
    note: Local stdio server — tools/list cannot be introspected over the network, so the tool set was read from
      the published Apache-2.0 source rather than from a live handshake.
  graphql: null
module_gating:
  variable: ENABLE_MODULES
  required: true
  behaviour: Without ENABLE_MODULES the server advertises only `list_modules` and NO API tools. tools/list returns
    only the modules the operator opted into.
  modules: 19
  consequence: Tool availability is an operator configuration decision, not a property of the server. Any agent-readiness
    measurement of this surface must state which modules were enabled.
coverage:
  mcp_tools: 109
  rest_operations: 108
  operations_reachable_via_mcp: 106
  operations_not_reachable_via_mcp: 2
  tools_without_a_rest_operation: 0
crosswalk:
- tool: astronomy_lookup
  module: weather
  annotation: read-only
  rest:
  - getAstronomyData
  binding: one-to-one
  confidence: high
  http:
  - GET /v2.0/geolocation/astronomy
- tool: commodity_fluctuation
  module: commodity
  annotation: read-only
  rest:
  - getCommodityFluctuation
  binding: one-to-one
  confidence: high
  http:
  - GET /v1.0/commodity/fluctuation
- tool: commodity_historical_rates
  module: commodity
  annotation: read-only
  rest:
  - getHistoricalCommodityPrices
  binding: one-to-one
  confidence: high
  http:
  - GET /v1.0/commodity/rates/historical
- tool: commodity_latest_rates
  module: commodity
  annotation: read-only
  rest:
  - getLatestCommodityPrices
  binding: one-to-one
  confidence: high
  http:
  - GET /v1.0/commodity/rates/latest
- tool: commodity_quotes
  module: commodity
  annotation: read-only
  rest:
  - getSupportedCurrencies
  binding: shared-operation
  confidence: high
  http:
  - GET /v1.0/currency/supported
  note: A convenience view over an operation another tool also calls; the tool narrows or validates one item from
    the same response.
- tool: commodity_symbol_info
  module: commodity
  annotation: read-only
  rest:
  - getCommoditySymbols
  binding: shared-operation
  confidence: high
  http:
  - GET /v1.0/commodity/symbols
  note: A convenience view over an operation another tool also calls; the tool narrows or validates one item from
    the same response.
- tool: commodity_symbols
  module: commodity
  annotation: read-only
  rest:
  - getCommoditySymbols
  binding: shared-operation
  confidence: high
  http:
  - GET /v1.0/commodity/symbols
  note: A convenience view over an operation another tool also calls; the tool narrows or validates one item from
    the same response.
- tool: commodity_time_series
  module: commodity
  annotation: read-only
  rest:
  - getCommodityTimeSeries
  binding: one-to-one
  confidence: high
  http:
  - GET /v1.0/commodity/time-series
- tool: currency_fluctuation
  module: currency
  annotation: read-only
  rest:
  - getFluctuation
  binding: one-to-one
  confidence: high
  http:
  - GET /v1.0/currency/fluctuation
- tool: currency_geo_convert
  module: currency
  annotation: read-only
  rest:
  - convertByIp
  binding: one-to-one
  confidence: high
  http:
  - GET /v1.0/currency/converter/ip-to-currency
- tool: currency_historical_converter
  module: currency
  annotation: read-only
  rest:
  - convertHistorical
  binding: one-to-one
  confidence: high
  http:
  - GET /v1.0/currency/converter/historical/prices
- tool: currency_historical_data_limit_info
  module: currency
  annotation: read-only
  rest:
  - getHistoricalDataLimits
  binding: shared-operation
  confidence: high
  http:
  - GET /v1.0/currency/historical/data/limits
  note: A convenience view over an operation another tool also calls; the tool narrows or validates one item from
    the same response.
- tool: currency_historical_data_limits
  module: currency
  annotation: read-only
  rest:
  - getHistoricalDataLimits
  binding: shared-operation
  confidence: high
  http:
  - GET /v1.0/currency/historical/data/limits
  note: A convenience view over an operation another tool also calls; the tool narrows or validates one item from
    the same response.
- tool: currency_historical_rates
  module: currency
  annotation: read-only
  rest:
  - getHistoricalExchangeRates
  binding: one-to-one
  confidence: high
  http:
  - GET /v1.0/currency/rates/historical
- tool: currency_latest_converter
  module: currency
  annotation: read-only
  rest:
  - convertLatest
  binding: one-to-one
  confidence: high
  http:
  - GET /v1.0/currency/converter/latest/prices
- tool: currency_latest_rates
  module: currency
  annotation: read-only
  rest:
  - getLatestExchangeRates
  binding: one-to-one
  confidence: high
  http:
  - GET /v1.0/currency/rates/latest
- tool: currency_supported
  module: currency
  annotation: read-only
  rest:
  - getSupportedCurrencies
  binding: shared-operation
  confidence: high
  http:
  - GET /v1.0/currency/supported
  note: A convenience view over an operation another tool also calls; the tool narrows or validates one item from
    the same response.
- tool: currency_symbol_info
  module: currency
  annotation: read-only
  rest:
  - getCurrencySymbols
  binding: shared-operation
  confidence: high
  http:
  - GET /v1.0/currency/symbols
  note: A convenience view over an operation another tool also calls; the tool narrows or validates one item from
    the same response.
- tool: currency_symbols
  module: currency
  annotation: read-only
  rest:
  - getCurrencySymbols
  binding: shared-operation
  confidence: high
  http:
  - GET /v1.0/currency/symbols
  note: A convenience view over an operation another tool also calls; the tool narrows or validates one item from
    the same response.
- tool: currency_time_series
  module: currency
  annotation: read-only
  rest:
  - getTimeSeries
  binding: one-to-one
  confidence: high
  http:
  - GET /v1.0/currency/time-series
- tool: dns_bulk_lookup
  module: dns
  annotation: read-only
  rest:
  - bulkDnsLookup
  binding: one-to-one
  confidence: high
  http:
  - POST /v1.0/domain/dns/live
- tool: dns_history
  module: dns
  annotation: read-only
  rest:
  - dnsHistoryLookup
  binding: one-to-one
  confidence: high
  http:
  - GET /v1.0/domain/dns/history
- tool: dns_lookup
  module: dns
  annotation: read-only
  rest:
  - dnsLookup
  binding: one-to-one
  confidence: high
  http:
  - GET /v1.0/domain/dns/live
- tool: dns_reverse
  module: dns
  annotation: read-only
  rest:
  - reverseDnsLookup
  binding: one-to-one
  confidence: high
  http:
  - GET /v1.0/domain/dns/reverse
- tool: domain_bulk_check_availability
  module: domain
  annotation: read-only
  rest:
  - checkBulkDomainAvailability
  binding: one-to-one
  confidence: high
  http:
  - POST /v1.0/domain/availability
- tool: domain_check_availability
  module: domain
  annotation: read-only
  rest:
  - checkDomainAvailability
  binding: one-to-one
  confidence: high
  http:
  - GET /v1.0/domain/availability
- tool: domain_check_availability_with_suggestions
  module: domain
  annotation: read-only
  rest:
  - suggestDomainAvailability
  binding: one-to-one
  confidence: high
  http:
  - GET /v1.0/domain/availability/suggestions
- tool: domain_subdomain_lookup
  module: domain
  annotation: read-only
  rest:
  - lookupSubdomains
  binding: one-to-one
  confidence: high
  http:
  - GET /v1.0/subdomains/lookup
- tool: email_bulk_validate
  module: email-validation
  annotation: read-only
  rest:
  - bulkCheckEmail
  binding: one-to-one
  confidence: high
  http:
  - POST /v1.0/email-validation/bulk
- tool: email_validate
  module: email-validation
  annotation: read-only
  rest:
  - checkEmail
  binding: one-to-one
  confidence: high
  http:
  - POST /v1.0/email-validation/single
- tool: financial_iban_validate
  module: financial
  annotation: read-only
  rest:
  - validateIban
  binding: one-to-one
  confidence: high
  http:
  - GET /v1.0/iban/validation
- tool: financial_supported_countries
  module: financial
  annotation: read-only
  rest:
  - getSupportedCountries
  binding: shared-operation
  confidence: high
  http:
  - GET /v1.0/vat/supported-countries
  note: A convenience view over an operation another tool also calls; the tool narrows or validates one item from
    the same response.
- tool: financial_supported_country_info
  module: financial
  annotation: read-only
  rest:
  - getSupportedCountries
  binding: shared-operation
  confidence: high
  http:
  - GET /v1.0/vat/supported-countries
  note: A convenience view over an operation another tool also calls; the tool narrows or validates one item from
    the same response.
- tool: financial_swift_finder
  module: financial
  annotation: read-only
  rest:
  - findSwiftCode
  binding: one-to-one
  confidence: high
  http:
  - GET /v1.0/swift-code/finder
- tool: financial_swift_lookup
  module: financial
  annotation: read-only
  rest:
  - lookupSwiftCode
  binding: one-to-one
  confidence: high
  http:
  - GET /v1.0/swift-code/lookup
- tool: financial_vat_rates_bulk
  module: financial
  annotation: read-only
  rest:
  - bulkVatRatesByCountry
  binding: one-to-one
  confidence: high
  http:
  - POST /v1.0/vat/rates/country
- tool: financial_vat_rates_by_country
  module: financial
  annotation: read-only
  rest:
  - getVatRatesByCountry
  binding: one-to-one
  confidence: high
  http:
  - GET /v1.0/vat/rates/country
- tool: financial_vat_rates_by_ip
  module: financial
  annotation: read-only
  rest:
  - getVatRatesByIp
  binding: one-to-one
  confidence: high
  http:
  - GET /v1.0/vat/rates/ip-address
- tool: financial_vat_validate
  module: financial
  annotation: read-only
  rest:
  - validateVatNumber
  binding: one-to-one
  confidence: high
  http:
  - GET /v1.0/vat/validation
- tool: geocode_forward
  module: geocoding
  annotation: read-only
  rest:
  - forwardGeocode
  binding: one-to-one
  confidence: high
  http:
  - GET /v1.0/geocoder/search
- tool: geocode_reverse
  module: geocoding
  annotation: read-only
  rest:
  - reverseGeocode
  binding: one-to-one
  confidence: high
  http:
  - GET /v1.0/geocoder/reverse
- tool: geodb_admin_levels
  module: geodb
  annotation: read-only
  rest:
  - getAdminLevels
  binding: one-to-one
  confidence: high
  http:
  - GET /v1.0/geo/admin-levels
- tool: geodb_admin_unit_details
  module: geodb
  annotation: read-only
  rest:
  - getAdminUnitDetails
  binding: one-to-one
  confidence: high
  http:
  - GET /v1.0/geo/admin-unit/details
- tool: geodb_admin_units
  module: geodb
  annotation: read-only
  rest:
  - getAdminUnits
  binding: one-to-one
  confidence: high
  http:
  - GET /v1.0/geo/admin-units
- tool: geodb_cities
  module: geodb
  annotation: read-only
  rest:
  - getCities
  binding: one-to-one
  confidence: high
  http:
  - GET /v1.0/geo/cities
- tool: geodb_countries
  module: geodb
  annotation: read-only
  rest:
  - getCountries
  binding: one-to-one
  confidence: high
  http:
  - GET /v1.0/geo/countries
- tool: geodb_country_details
  module: geodb
  annotation: read-only
  rest:
  - getCountryDetails
  binding: one-to-one
  confidence: high
  http:
  - GET /v1.0/geo/country/details
- tool: geodb_flag
  module: geodb
  annotation: read-only
  rest:
  - getFlag
  binding: one-to-one
  confidence: high
  http:
  - GET /v1.0/flags
- tool: geodb_flags_supported
  module: geodb
  annotation: read-only
  rest:
  - getSupportedFlags
  binding: one-to-one
  confidence: high
  http:
  - GET /v1.0/flags/supported
- tool: geodb_regions
  module: geodb
  annotation: read-only
  rest:
  - getRegions
  binding: one-to-one
  confidence: high
  http:
  - GET /v1.0/geo/regions
- tool: geodb_subregions
  module: geodb
  annotation: read-only
  rest:
  - getSubregionsByRegion
  binding: one-to-one
  confidence: high
  http:
  - GET /v1.0/geo/subregions
- tool: ip_security_bulk_lookup
  module: ip-intelligence
  annotation: read-only
  rest:
  - bulkIpSecurityLookup
  binding: one-to-one
  confidence: high
  http:
  - POST /v1.0/ip/security
- tool: ip_security_lookup
  module: ip-intelligence
  annotation: read-only
  rest:
  - ipSecurityLookup
  binding: one-to-one
  confidence: high
  http:
  - GET /v1.0/ip/security
- tool: ipgeolocation_bulk_lookup
  module: ip-intelligence
  annotation: read-only
  rest:
  - bulkIpLookup
  binding: one-to-one
  confidence: high
  http:
  - POST /v2.0/geolocation/lookup
- tool: ipgeolocation_lookup
  module: ip-intelligence
  annotation: read-only
  rest:
  - ipLookup
  binding: one-to-one
  confidence: high
  http:
  - GET /v2.0/geolocation/lookup
- tool: pdf_compress
  module: pdf
  annotation: write
  rest:
  - compressPdf
  binding: one-to-one
  confidence: high
  http:
  - GET /v1.0/pdf/compress
- tool: pdf_decrypt
  module: pdf
  annotation: write
  rest:
  - decryptPdf
  binding: one-to-one
  confidence: high
  http:
  - GET /v1.0/pdf/decrypt
- tool: pdf_encrypt
  module: pdf
  annotation: write
  rest:
  - encryptPdf
  binding: one-to-one
  confidence: high
  http:
  - GET /v1.0/pdf/encrypt
- tool: pdf_extract_pages
  module: pdf
  annotation: write
  rest:
  - extractPdfPages
  binding: one-to-one
  confidence: high
  http:
  - GET /v1.0/pdf/extract-pages
- tool: pdf_file_delete
  module: pdf
  annotation: destructive
  rest:
  - deleteFile
  binding: one-to-one
  confidence: high
  http:
  - DELETE /v1.0/pdf/file
- tool: pdf_file_status
  module: pdf
  annotation: read-only
  rest:
  - getFileStatus
  binding: one-to-one
  confidence: high
  http:
  - GET /v1.0/pdf/file-status
- tool: pdf_files
  module: pdf
  annotation: read-only
  rest:
  - getFiles
  binding: one-to-one
  confidence: high
  http:
  - GET /v1.0/pdf/files
- tool: pdf_generate
  module: pdf
  annotation: write
  rest:
  - generatePdfFromTemplate
  binding: one-to-one
  confidence: high
  http:
  - POST /v1.0/pdf/template/generate
- tool: pdf_generate_bulk
  module: pdf
  annotation: write
  rest:
  - generatePdfsFromCsv
  binding: one-to-one
  confidence: high
  http:
  - POST /v1.0/pdf/template/generate/bulk
- tool: pdf_linearize
  module: pdf
  annotation: write
  rest:
  - linearizePdf
  binding: one-to-one
  confidence: high
  http:
  - GET /v1.0/pdf/linearize
- tool: pdf_merge
  module: pdf
  annotation: write
  rest:
  - mergePdf
  binding: one-to-one
  confidence: high
  http:
  - GET /v1.0/pdf/merge
- tool: pdf_remove_pages
  module: pdf
  annotation: write
  rest:
  - deletePdfPage
  binding: one-to-one
  confidence: high
  http:
  - GET /v1.0/pdf/remove-pages
- tool: pdf_restrict
  module: pdf
  annotation: write
  rest:
  - restrictPdf
  binding: one-to-one
  confidence: high
  http:
  - GET /v1.0/pdf/restrict
- tool: pdf_rotate
  module: pdf
  annotation: write
  rest:
  - rotatePdfPages
  binding: one-to-one
  confidence: high
  http:
  - GET /v1.0/pdf/rotate
- tool: pdf_split
  module: pdf
  annotation: write
  rest:
  - splitPdf
  binding: one-to-one
  confidence: high
  http:
  - GET /v1.0/pdf/split
- tool: pdf_task_status
  module: pdf
  annotation: read-only
  rest:
  - getTaskStatus
  binding: one-to-one
  confidence: high
  http:
  - GET /v1.0/pdf/task-status
- tool: pdf_to_image
  module: pdf
  annotation: write
  rest:
  - pdfToImage
  - (unnamed) /v1.0/pdf/jpg|POST
  - (unnamed) /v1.0/pdf/tif|POST
  - (unnamed) /v1.0/pdf/bmp|POST
  - (unnamed) /v1.0/pdf/gif|POST
  binding: one-to-many
  confidence: high
  http:
  - GET /v1.0/pdf/png
  - GET /v1.0/pdf/jpg
  - GET /v1.0/pdf/tif
  - GET /v1.0/pdf/bmp
  - GET /v1.0/pdf/gif
  note: Chooses one of five format-specific REST endpoints (/pdf/png|jpg|tif|bmp|gif) from the `format` argument;
    four of those operations carry NO operationId upstream and are listed by path.
- tool: pdf_unrestrict
  module: pdf
  annotation: write
  rest:
  - unrestrictPdf
  binding: one-to-one
  confidence: high
  http:
  - GET /v1.0/pdf/unrestrict
- tool: pdf_upload
  module: pdf
  annotation: write
  rest:
  - uploadResource
  binding: one-to-one
  confidence: high
  http:
  - POST /v1.0/pdf/resource/upload
- tool: pdf_upload_binary
  module: pdf
  annotation: write
  rest:
  - uploadResourceBinary
  binding: one-to-one
  confidence: high
  http:
  - POST /v1.0/pdf/resource/upload-binary
- tool: phone_bulk_validate
  module: phone-validation
  annotation: read-only
  rest:
  - bulkValidatePhoneNumbers
  binding: one-to-one
  confidence: high
  http:
  - POST /v1.0/phone/validation/bulk
- tool: phone_validate
  module: phone-validation
  annotation: read-only
  rest:
  - validatePhoneNumber
  binding: one-to-one
  confidence: high
  http:
  - POST /v1.0/phone/validation
- tool: scraper_scrape
  module: scraper
  annotation: read-only
  rest:
  - performScraping
  binding: shared-operation
  confidence: high
  http:
  - POST /v1.0/scraping
  note: A convenience view over an operation another tool also calls; the tool narrows or validates one item from
    the same response.
- tool: scraper_scrape_js
  module: scraper
  annotation: read-only
  rest:
  - performScraping
  binding: shared-operation
  confidence: high
  http:
  - POST /v1.0/scraping
  note: A convenience view over an operation another tool also calls; the tool narrows or validates one item from
    the same response.
- tool: screenshot_bulk_capture
  module: screenshot
  annotation: read-only
  rest:
  - bulkScreenshot
  binding: one-to-one
  confidence: high
  http:
  - POST /v1.0/screenshot
- tool: screenshot_capture
  module: screenshot
  annotation: read-only
  rest:
  - websiteScreenshot
  binding: shared-operation
  confidence: high
  http:
  - GET /v1.0/screenshot
  note: A convenience view over an operation another tool also calls; the tool narrows or validates one item from
    the same response.
- tool: screenshot_capture_scrolling
  module: screenshot
  annotation: read-only
  rest:
  - websiteScreenshot
  binding: shared-operation
  confidence: high
  http:
  - GET /v1.0/screenshot
  note: A convenience view over an operation another tool also calls; the tool narrows or validates one item from
    the same response.
- tool: ssl_live_chain_lookup
  module: ssl
  annotation: read-only
  rest:
  - sslCertificateChainLookup
  binding: one-to-one
  confidence: high
  http:
  - GET /v1.0/domain/ssl/live/chain
- tool: ssl_live_lookup
  module: ssl
  annotation: read-only
  rest:
  - sslCertificateLookup
  binding: one-to-one
  confidence: high
  http:
  - GET /v1.0/domain/ssl/live
- tool: timezone_convert
  module: timezone
  annotation: read-only
  rest:
  - convertTimezone
  binding: one-to-one
  confidence: high
  http:
  - GET /v1.0/timezone/converter
- tool: timezone_lookup
  module: timezone
  annotation: read-only
  rest:
  - getTimezone
  binding: one-to-one
  confidence: high
  http:
  - GET /v2.0/geolocation/timezone
- tool: user_agent_bulk_parse
  module: user-agent
  annotation: read-only
  rest:
  - bulkParseUserAgent
  binding: one-to-one
  confidence: high
  http:
  - POST /v1.0/user-agent/lookup
- tool: user_agent_parse
  module: user-agent
  annotation: read-only
  rest:
  - parseUserAgent
  binding: one-to-one
  confidence: high
  http:
  - GET /v1.0/user-agent/lookup
- tool: weather_air_quality
  module: weather
  annotation: read-only
  rest:
  - getAirQuality
  binding: one-to-one
  confidence: high
  http:
  - GET /v1.0/weather/air-quality
- tool: weather_bulk_current
  module: weather
  annotation: read-only
  rest:
  - bulkLiveWeather
  binding: one-to-one
  confidence: high
  http:
  - POST /v1.0/weather/current
- tool: weather_current
  module: weather
  annotation: read-only
  rest:
  - getLiveWeather
  binding: one-to-one
  confidence: high
  http:
  - GET /v1.0/weather/current
- tool: weather_flood_forecast
  module: weather
  annotation: read-only
  rest:
  - getFloodForecast
  binding: one-to-one
  confidence: high
  http:
  - GET /v1.0/weather/flood
- tool: weather_forecast
  module: weather
  annotation: read-only
  rest:
  - getWeatherForecast
  binding: one-to-one
  confidence: high
  http:
  - GET /v1.0/weather/forecast
- tool: weather_historical
  module: weather
  annotation: read-only
  rest:
  - getHistoricalData
  binding: one-to-one
  confidence: high
  http:
  - GET /v1.0/weather/historical
- tool: weather_marine
  module: weather
  annotation: read-only
  rest:
  - getMarineWeather
  binding: one-to-one
  confidence: high
  http:
  - GET /v1.0/weather/marine
- tool: weather_time_series
  module: weather
  annotation: read-only
  rest:
  - getTimeSeriesData
  binding: one-to-one
  confidence: high
  http:
  - GET /v1.0/weather/time-series
- tool: whois_asn_lookup
  module: whois
  annotation: read-only
  rest:
  - asnLookup
  binding: one-to-one
  confidence: high
  http:
  - GET /v1.0/asn/whois/live
- tool: whois_bulk_domain_lookup
  module: whois
  annotation: read-only
  rest:
  - bulkWhoisLookup
  binding: one-to-one
  confidence: high
  http:
  - POST /v2.0/domain/whois/live
- tool: whois_domain_history
  module: whois
  annotation: read-only
  rest:
  - whoisHistoryLookup
  binding: one-to-one
  confidence: high
  http:
  - GET /v1.0/domain/whois/history
- tool: whois_domain_lookup
  module: whois
  annotation: read-only
  rest:
  - lookupDomainWhois
  binding: one-to-one
  confidence: high
  http:
  - GET /v2.0/domain/whois/live
- tool: whois_ip_lookup
  module: whois
  annotation: read-only
  rest:
  - lookupIpWhois
  binding: one-to-one
  confidence: high
  http:
  - GET /v1.0/ip/whois/live
- tool: whois_reverse_lookup
  module: whois
  annotation: read-only
  rest:
  - reverseWhoisLookup
  binding: one-to-one
  confidence: high
  http:
  - GET /v1.0/domain/whois/reverse
- tool: zipcode_bulk_lookup
  module: zipcode
  annotation: read-only
  rest:
  - bulkLookupZipCodes
  binding: one-to-one
  confidence: high
  http:
  - POST /v1.0/zipcode/lookup
- tool: zipcode_by_city
  module: zipcode
  annotation: read-only
  rest:
  - searchZipByCity
  binding: one-to-one
  confidence: high
  http:
  - GET /v1.0/zipcode/search/city
- tool: zipcode_by_region
  module: zipcode
  annotation: read-only
  rest:
  - searchZipByRegion
  binding: one-to-one
  confidence: high
  http:
  - GET /v1.0/zipcode/search/region
- tool: zipcode_distance
  module: zipcode
  annotation: read-only
  rest:
  - getZipcodeDistance
  binding: one-to-one
  confidence: high
  http:
  - POST /v1.0/zipcode/distance
- tool: zipcode_distance_match
  module: zipcode
  annotation: read-only
  rest:
  - getZipcodeDistanceMatch
  binding: one-to-one
  confidence: high
  http:
  - POST /v1.0/zipcode/distance/match
- tool: zipcode_lookup
  module: zipcode
  annotation: read-only
  rest:
  - lookupZipCode
  binding: one-to-one
  confidence: high
  http:
  - GET /v1.0/zipcode/lookup
- tool: zipcode_radius_search
  module: zipcode
  annotation: read-only
  rest:
  - searchZipByRadius
  binding: one-to-one
  confidence: high
  http:
  - GET /v1.0/zipcode/search/radius
mcp_only: []
rest_only:
- operationId: downloadResource
  reason: Binary download of a stored PDF resource. The MCP server returns the hosted download URL from the job/tool
    response instead of proxying bytes through the transport.
- operationId: getCreditsUsage
  reason: Account credit-balance endpoint. Not exposed as a tool — an agent cannot ask the server how many credits
    are left before spending them.
x-evidence:
  fetched: '2026-09-04'
  urls:
  - url: https://raw.githubusercontent.com/api-freaks/apifreaks-mcp/main/src/endpoints.ts
    http_status: 200
  - url: https://registry.npmjs.org/@apifreaks/mcp
    http_status: 200
    latest: 2.0.1
    published: '2026-08-24'
  - url: https://raw.githubusercontent.com/api-freaks/apifreaks-mcp/main/server.json
    http_status: 200
